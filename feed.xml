---
---
<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" {% if site.lang %}xml:lang="{{ site.lang }}"{% endif %}>
  <generator uri="https://jekyllrb.com/" version="{{ jekyll.version }}">Jekyll</generator>
  <link href="{{ page.url | absolute_url }}" rel="self" type="application/atom+xml" />
  <link href="{{ '/' | absolute_url }}" rel="alternate" type="text/html" {% if site.lang %}hreflang="{{ site.lang }}" {% endif %}/>
  <updated>{{ site.time | date_to_xmlschema }}</updated>
  <id>{{ page.url | absolute_url | xml_escape }}</id>

  {% assign title = site.title | default: site.name %}
  {% if page.collection != "posts" %}
    {% assign collection = page.collection | capitalize %}
    {% assign title = title | append: " | " | append: collection %}
  {% endif %}
  {% if page.category %}
    {% assign category = page.category | capitalize %}
    {% assign title = title | append: " | " | append: category %}
  {% endif %}

  {% if title %}
    <title type="html">{{ title | smartify | xml_escape }}</title>
  {% endif %}

  {% if site.description %}
    <subtitle>{{ site.description | xml_escape }}</subtitle>
  {% endif %}

  {% if site.author %}
    <author>
        <name>{{ site.author.name | default: site.author | xml_escape }}</name>
      {% if site.author.email %}
        <email>{{ site.author.email | xml_escape }}</email>
      {% endif %}
      {% if site.author.uri %}
        <uri>{{ site.author.uri | xml_escape }}</uri>
      {% endif %}
    </author>
  {% endif %}

  {% for short-stories in site.short-stories %}
    <entry>
      <title type="html">{{ short-stories.title | xml_escape }}</title>
			{% if short-stories.subtitle %}
				<subtitle type="html">{{ short-stories.subtitle | xml_escape }}</subtitle>
			{% endif %}
      <link href="{{ short-stories.url | absolute_url }}" rel="alternate" type="text/html" title="{{ short-stories.title | xml_escape }}" />
      <published>{{ short-stories.date | date_to_xmlschema }}</published>
      <id>{{ short-stories.casefileNumber | xml_escape }}</id>
      <content type="html" xml:base="{{ short-stories.url | absolute_url | xml_escape }}">{{ short-stories.content | strip | xml_escape }}</content>
      <author>
				{% if short-stories.author %}
          <name>{{ short-stories.author | xml_escape }}</name>
				{% else %}
					<name>{{ short-stories.series | xml_escape }}</name>
				{% endif %}
      </author>

      {% if short-stories.category %}
        <category genre="{{ short-stories.genre | xml_escape }}" />
      {% endif %}

      {% for tag in short-stories.tags %}
        <category term="{{ tag | xml_escape }}" />
      {% endfor %}

				<category opub="{{ short-stories.opub | xml_escape }}" />
				<category opubdate="{{ short-stories.opubdate | xml_escape }}" />
				<category opub="{{ short-stories.opubissue | xml_escape }}" />
				<category wordcount="{{ short-stories.wordcount | xml_escape }}" />

      {% if short-stories.excerpt and short-stories.excerpt != empty %}
        <summary type="html">{{ short-stories.excerpt | strip_html | normalize_whitespace | xml_escape }}</summary>
      {% endif %}

        <media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://www.talesofmurder.com{{ short-stories.image | xml_escape }}" />

    </entry>
  {% endfor %}
</feed>